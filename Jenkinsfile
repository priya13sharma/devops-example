pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                echo "HelloWorld"
            }
        }
         stage('Test') {
            steps {
                echo "HelloWorld"
                
            }
        }
        
    }
}
